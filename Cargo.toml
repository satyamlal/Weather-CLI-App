[package]
name = "Weather-CLI-App"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = {version = "0.1", feature = ["blocking", "json"]}
serde = {version = "1.0", feature = ["derive"]}
serde_json = "1.0"
colored = "2.0"